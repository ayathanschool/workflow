{
  "version": 2,
  "buildCommand": "npm ci && npm run build",
  "outputDirectory": "dist",
  "devCommand": "vite",
  "installCommand": "npm ci",
  "framework": "vite",
  "routes": [
    { "src": "/[^.]+", "dest": "/index.html" }
  ]
}
